apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base Kustomization for Airbyte
# Use this if you prefer raw manifests over Helm

namespace: airbyte

resources:
  # Add base manifest files here
  # - manifests/deployment.yaml
  # - manifests/service.yaml
  # etc.

commonLabels:
  app.kubernetes.io/name: airbyte
  app.kubernetes.io/managed-by: kustomize

# Common annotations
commonAnnotations:
  managed-by: "airbyte-solutions-deployments"

