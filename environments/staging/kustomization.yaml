apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: airbyte

bases:
  - ../../base

namePrefix: staging-

commonLabels:
  environment: staging
  app.kubernetes.io/instance: airbyte-staging

# Environment-specific resources
resources:
  # Add staging-specific resources here

# Patches for staging environment
patchesStrategicMerge:
  # - patches/increase-replicas.yaml

