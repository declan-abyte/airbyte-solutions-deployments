apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: airbyte

bases:
  - ../../base

namePrefix: dev-

commonLabels:
  environment: dev
  app.kubernetes.io/instance: airbyte-dev

# Environment-specific resources
resources:
  # Add dev-specific resources here

# Patches for dev environment
patchesStrategicMerge:
  # - patches/reduce-replicas.yaml

